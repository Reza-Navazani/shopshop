[defaults]
group = YourResourceGroup
sku = P1v2
appserviceplan = YourAppServicePlan
location = westus2
web = dynamic-server

[setup]
app_command = gunicorn --bind=0.0.0.0:8000 --timeout 600 --workers 2 wsgi:app